def strategy_variables_model_run(strategy_variables):
    """Collect all strategy values for the specific model run
    """
    try:

    except KeyError:
        logging.warning("Not all scenario variables are given as inputs")


